# HANDOFF for Antigravity CLI (agy)

This document ensures session continuity for the **Teenieping Quiz Service** project when using `agy`.

## 📌 Context Overview
- **Project Goal**: A static web-based quiz game for naming Teenieping characters, using data scraped from NamuWiki.
- **Current State**: Production-ready V1. Data for 145 characters (Season 1-6) is fully processed. UI is optimized for mobile/PC with a 100vh pink theme.
- **Key Files**:
  - `web/app.js`: Core game logic, state management, and mobile audio handling.
  - `web/styles.css`: Teenieping Bright Pink Theme with responsive scaling.
  - `data/processed/characters.json`: The final source of truth for character data.
  - `scripts/build_dataset.mjs`: Node.js script for parsing HTML and generating the JSON dataset.

## 🚀 Recent Key Changes (2026-06-16)
1.  **Thematic UI Overhaul**: Replaced the initial Apple-style design with a vibrant pink-toned "Teenieping" aesthetic.
2.  **Viewport Optimization**: Implemented a strict `100vh` layout to prevent scrolling on mobile devices.
3.  **Mobile Audio Fix**: Added `async/await` logic to unlock `AudioContext` on user gesture (first click) to overcome iOS/Android browser restrictions.
4.  **Scaling Fix**: Ensured all character images are strictly contained within their designated cards using `object-fit: contain`.

## ⚠️ Known Issues / Technical Debt
- **iOS Audio**: Despite several aggressive unlocking attempts, audio may still be silent on some iPhones (likely due to the physical silent mode switch or specific Safari versions).
- **Voice Assets**: Metadata is ready (`voice-sources.json`), but actual MP3 clips are not yet harvested/linked.

## 🛠 Next Development Steps (for `agy`)
1.  **Voice Asset Harvesting**: Automate or guide the extraction of MP3 clips from YouTube sources defined in `data/manual/voice-sources.json`.
2.  **Sound Debugging**: If requested, perform deeper debugging of the Web Audio API specifically for iOS WebViews.
3.  **V2 Features**: Implement the "Collection Book" or "Social Sharing" features mentioned in `PLAN.md`.

---
*Generated by Gemini CLI to assist Antigravity CLI (agy) transition.*
